package com.virtual.library.service;

import org.springframework.stereotype.Service;

@Service
public class AuthorServiceImpl implements AuthorService {
    //логика работы с сущностью автора (при необходимости)
}